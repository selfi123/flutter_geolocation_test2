import 'package:flutter/material.dart';

class UserFunctions extends StatelessWidget {
  const UserFunctions({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
